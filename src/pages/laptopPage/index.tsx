import React from "react";
import LaptopContainer from "../../containers/laptopContainer";
import "./style.css";

const LaptopPage = () => {
    return (
        <LaptopContainer />
    );
};
export default React.memo(LaptopPage);
